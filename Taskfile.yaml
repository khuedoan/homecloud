version: '3'

vars:
  ENV: '{{.ENV | default "staging"}}'
dotenv:
  - .env
includes:
  global:
    taskfile: ./global/Taskfile.yaml
    dir: ./global
tasks:
  default:
    cmds:
      - task: global:init
      - task: infra
